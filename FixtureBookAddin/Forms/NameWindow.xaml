<Window x:Class="XPFriend.FixtureBook.Forms.NameWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:properties="clr-namespace:XPFriend.FixtureBook.Properties"
             Height="120" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Content="{x:Static properties:Resources.NameLabel}" HorizontalAlignment="Right" />
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="ConnectionNameText"/>
        <Button Grid.Row="1" Grid.Column="1" x:Name="AddButton" Content="{x:Static properties:Resources.AddButton}" HorizontalAlignment="Right" Margin="10" Padding="4" IsDefault="True" Click="AddButton_Click" />
    </Grid>
</Window>
